---
description: SKIIS 上下文管理系统 — 会话上下文加载、保存、健康检查、自动初始化
globs: **/*
alwaysApply: true
---

# 上下文管理系统

你是全局上下文管理器。以下规则在任何项目中**自动生效**，无需用户手动触发任何命令。

## 规则 0：智能加载（最高优先级）

**先理解问题，再加载上下文。绝不盲目加载。**

```text
用户提问
  │
  ├─ 用户提到 bug/修复/fix/debug/问题/报错？
  │     │
  │     ├─ 是 → 立即执行 ls .bugs/（不要先读其他文件）
  │     │     ├─ .bugs/ 有文件 → 直接开始处理（解压/解析/修复）
  │     │     └─ .bugs/ 为空 → 告诉用户放入 Bug 文件
  │     │
  │     └─ 否 → 继续下面的流程
  │
  ├─ 项目简单？→ 直接加载当前目录的 .claude/context/ → 回答
  │
  └─ 项目复杂（有子模块）？
        ├─ 根据用户问题判断涉及哪个子模块
        ├─ 该子模块有多个分支？→ 列出来让用户选
        ├─ 单分支 → 直接加载
        └─ 回答用户（其他子模块不碰）
```

对话中途涉及新模块，同样流程。

**加载上下文后，必须执行：将当前任务写入 `current-task.md`**

无论用户说了什么，加载完上下文后**立即**将用户的请求写入 `current-task.md`：

```text
读取 current-task.md
  │
  ├─ 文件存在 → 检查是否有未完成任务
  │     ├─ 有未完成任务 → 在"进行中"追加新任务
  │     └─ 全部完成 → 清空旧任务，写入新任务
  │
  └─ 文件不存在 → 创建并写入新任务
```

写入格式：

```markdown
## 已完成
暂无

## 进行中
- [ ] {用户请求的具体任务描述}

## 待完成
暂无

## 关键上下文
- {与任务相关的模块、文件、约束}
```

**这一步是强制的——每次对话加载上下文后都必须执行，不需要用户手动触发。**

## 上下文文件系统

所有上下文存储在 `.claude/context/` 下：

| 文件 | 用途 | 更新策略 |
| ------ | ------ | --------- |
| `current-task.md` | 任务进度（checkbox 跟踪） | 覆盖更新 |
| `decisions.md` | 技术决策记录 | 追加 |
| `pitfalls.md` | 踩坑记录 | 追加 |
| `architecture.md` | 项目架构 | context-sync 生成 |
| `tasks/` | 跨项目排查任务 | 按需 |

## 规则 1：启动时自动加载上下文

每次新对话开始，如果用户没有提供明确任务，立即执行：

1. 检查 `.claude/context/current-task.md` 是否存在
2. 如果存在，读取并呈现摘要（已完成 / 进行中 / 待完成 / 关键约束）
3. 读取 `.claude/context/decisions.md`（最近 5 条）
4. 读取 `.claude/context/pitfalls.md`（最近 5 条）
5. **优先展示 `## 🔜 下次继续` 段落**（这是最重要的——告诉 AI 从哪里开始）
6. 询问："准备就绪，请告诉我需要做什么？"

## 规则 2：新项目自动初始化

当检测到 `.claude/context/` 目录不存在时，**不要询问，直接执行**：

1. 告知用户："检测到项目尚未初始化上下文系统，正在自动初始化..."
2. 扫描项目结构：配置文件、顶层目录、源码文件
3. 创建 `.claude/context/` 并生成模板文件（current-task.md、decisions.md、pitfalls.md）
4. 如果项目有源码，自动生成 `architecture.md`
5. 报告初始化结果

## 规则 3：对话疲劳自动预警

- 对话超过 15 轮时，主动提醒保存进度
- 发现自身回复矛盾、不确定文件路径、重复建议时立即警告

## 规则 3.5：Bug 修复请求自动响应

当用户提到"bug"、"修复"、"fix"、"debug"、"问题"、"报错"等关键词时，**立即检查项目根目录是否存在 `.bugs/` 目录**：

```text
用户提到 bug 相关关键词
  │
  ├─ 检查 .bugs/ 目录
  │     │
  │     ├─ 存在且有文件 → 立即开始处理：
  │     │     1. 扫描 .bugs/ 中的文件
  │     │     2. 解压压缩包（zip/rar/7z）
  │     │     3. 解析 PDF 文档
  │     │     4. 读取文本文件
  │     │     5. 提取 Bug 列表
  │     │     6. 按根因聚类
  │     │     7. 开始逐个修复
  │     │
  │     │     不需要用户说"开始修 bug"，直接开始。
  │     │
  │     └─ 不存在或为空 → 告知用户：
  │           "项目中还没有 Bug 文件。请将 Bug 报告放入 `.bugs/` 目录，
  │            或直接描述你遇到的问题。"
  │
  └─ 不要反问"具体是哪个 bug"——如果 .bugs/ 有文件就直接处理
```

**关键：不要问"具体是哪个 bug"。** 如果 `.bugs/` 有文件，直接处理；如果没有，才让用户描述或放入文件。

同时，加载上下文时也检查 `.bugs/`：

```text
加载上下文后，检查 .bugs/ 目录是否有文件
  │
  ├─ 有 → 提示用户：
  │     "📂 检测到 .bugs/ 目录中有 N 个待处理的 Bug 文件，是否开始修复？"
  │
  └─ 无 → 继续正常流程
```

## 规则 4：工作产出自动归档

| 操作 | 行为 |
| ------ | ------ |
| 修复 bug | 提示记录根因和方案到 `pitfalls.md` |
| 技术决策 | 提示追加到 `decisions.md` |
| 完成功能模块 | 提示更新 `current-task.md` 进度 |
| 生成修改计划 | 自动转为 `current-task.md` 的 checkbox 清单 |

## 规则 5：保存时写入继续计划

保存进度时，必须在 `current-task.md` 中写入 `## 🔜 下次继续` 段落：

```markdown
## 🔜 下次继续
### 第一步
打开 {文件}，定位到 {函数/行号}，做 {具体操作}

### 当前状态
{编译/运行状态、写到哪了}

### 关键约束
{需要注意的限制}

### 阻塞项
{等待确认的事项}
```

要求：第一步必须具体到"打开 X 文件，定位到 Y 函数/行号，做 Z"，不能泛泛而谈。

### 规则 4.5：修改计划自动同步

当 AI 在开始工作前生成了修改计划（Plan Mode、重构方案、实施步骤等），**自动同步到 `current-task.md`**：

1. 将计划中的步骤转换为 checkbox 列表
2. 已完成项标记 `[x]`，待完成项标记 `[ ]`
3. 更新"关键上下文"部分（涉及的模块、文件、接口约束等）
4. 向用户确认："已将修改计划同步到 `current-task.md`"

无需用户手动运行 `/session-save`，计划自动归档。

### 规则 4.51：新任务自动捕获（会话内任务切换）

当用户在会话中完成当前任务后提出新任务时，**自动更新 `current-task.md`**：

**触发条件：**

- `current-task.md` 中所有 checkbox 已打勾（任务完成）
- 用户提出了新的需求/问题/任务
- 用户说"继续"、"还有"、"另外"、"接下来"、"再帮我"等衔接词

**自动执行：**

```text
用户提出新任务
  │
  ├─ 当前任务全部完成？
  │     │
  │     ├─ 是 → 自动将已完成任务归档：
  │     │     ├─ 提取关键技术决策 → 追加到 decisions.md
  │     │     ├─ 提取踩坑记录 → 追加到 pitfalls.md
  │     │     ├─ 提取关键上下文 → 移到 reference/
  │     │     └─ 清空 current-task.md 的任务部分
  │     │
  │     │     然后写入新任务：
  │     │     ├─ 将用户新需求写入 current-task.md
  │     │     ├─ 更新"关键上下文"部分
  │     │     └─ 向用户确认："已归档上个任务，新任务已记录到 current-task.md"
  │     │
  │     └─ 否 → 在现有任务列表末尾追加新任务
  │
  └─ 不需要用户手动运行 /session-save
```

**示例：**

上个任务完成后 `current-task.md`：

```markdown
## 已完成
- [x] 修复登录超时问题
- [x] 添加重试机制

## 进行中
暂无

## 待完成
暂无
```

用户说"还有个问题，首页加载很慢"，自动更新为：

```markdown
## 已完成
暂无

## 进行中
- [ ] 优化首页加载性能

## 待完成
暂无

## 关键上下文
- 上个任务：登录超时已修复，重试机制已添加（详见 reference/）
- 当前任务：首页加载慢，需排查性能瓶颈
```

### 规则 4.55：关键文件清单（跨会话上下文接力）

当上下文即将耗尽需要开新会话时，在 `current-task.md` 的 `## 🔜 下次继续` 段落后追加 `## 📂 关键文件清单`：

```markdown
## 📂 关键文件清单（新会话直接加载，无需重新探索）

### 问题定位相关
- src/ike/negotiate.c:240-350  ← SA 协商主逻辑，handle_timeout() 在这里
- src/ike/sa.h:45-78          ← SA 状态枚举定义

### 已排除的文件（不需要再看）
- src/ike/crypto.c            ← 加密模块，已验证正常

### 当前排查结论
- SA 状态机在超时后未正确重置 → handle_timeout() 缺少互斥锁
- 下一步：给 handle_timeout() 加互斥锁，验证死锁是否解决
```

**新会话 load 时：**

1. 读 `📂 关键文件清单` → 直接打开列出的文件（指定了行号范围）
2. 跳过"已排除"的文件
3. 读"当前排查结论" → 秒懂问题状态
4. **不需要重新探索项目结构、不需要重读无关代码**

### 规则 4.6：编译环境和工具安装自动归档

当项目涉及编译/构建环境，且需要安装工具链或依赖应用时：

1. **安装前**：将工具选择作为技术决策记录到 `decisions.md`
2. **安装后**：根据用户反馈分情况处理：
   - **安装成功** → 记录到 `decisions.md`（决策 ID、工具名、版本、安装命令）
   - **遇到问题** → 记录到 `pitfalls.md`（错误信息、根因、解决方案）
   - **换了方案** → 更新 `decisions.md`（记录原方案失败原因和新方案选择理由）

`decisions.md` 示例：

```markdown
## D{YYYY-MM-DD-N}: 编译环境 - {工具名}
- **日期**：{date}
- **选择**：{工具名} {版本号}
- **安装命令**：`{command}`
- **验证方式**：`{verify command}`
```

`pitfalls.md` 示例：

```markdown
## P{YYYY-MM-DD-N}: {工具名} 安装问题
- **现象**：{error message}
- **根因**：{why}
- **解决**：{steps}
- **命令**：`{command}`
```

### 规则 4.65：远程设备调试凭据管理

当用户需要调试远程设备（SSH、串口、telnet 等）时：

**记录阶段：**

1. 用户提供远程信息时，记录到 `current-task.md` 的 `## 关键上下文` 段落：

   ```markdown
   ## 🔐 远程调试（仅本次任务，完成后删除）
   - 方式：SSH
   - 地址：192.168.1.100:22
   - 账号：admin
   - 密码：xxxxxx
   ```

2. **绝不写入 `decisions.md`**（会被持久化）

**清理阶段：**

- 任务完成后，从 `current-task.md` 中删除远程凭据
- 只保留非敏感信息到 `reference/`：设备类型、网络拓扑、调试方式

**重要：绝不将账号密码写入 decisions.md、pitfalls.md、architecture.md。**

### 规则 4.8：分层上下文（大型多模块项目）

当项目根目录下有多个独立子功能目录时，使用**分层上下文**。

**初始化流程（根目录，只做一次）：**

```
/session-load（根目录，首次）
  │
  ├─ 完整扫描所有子目录，生成 architecture.md（含子模块清单）
  ├─ 询问："检测到以下子模块：A/B/C...，是否为它们也创建独立上下文？"
  │
  └─ 用户确认后，为每个子目录自动执行 mini-init
```

**嵌套支持：**

```
大型项目/
├── .claude/context/           # 项目级上下文
├── 子系统/
│   ├── .claude/context/       # 子系统级上下文
│   ├── 子模块A/
│   │   ├── .claude/context/   # 模块级上下文
│   │   └── 子模块A1/
│   │       └── .claude/context/  # 任意深度
```

每层独立运作。在哪层加载就加载哪层的上下文。任务可以在任意两层之间派发。

## 规则 6：架构变化自动提醒

检测到目录增删、依赖变更、技术方案切换时，主动提醒用户同步 `architecture.md`。

### 规则 6.5：上下文文件瘦身（防止负载膨胀）

`decisions.md` 和 `pitfalls.md` 持续追加会越来越长，load 时占用越来越多上下文。

**瘦身规则：**

| 条件 | 操作 |
| ------ | ------ |
| 文件超过 200 行 | 下次 load 时提示归档旧条目 |
| 条目超过 6 个月 | 自动移到 `-archive.md` |
| 用户说"归档" | 手动触发归档 |

**load 时的读取策略：**

| 文件 | 读取方式 | 原因 |
| ------ | ------ | ------ |
| `current-task.md` | 完整读取 | 覆盖更新，不会膨胀 |
| `decisions.md` | 只读最近 **10 条** | 旧决策基本不会再查 |
| `pitfalls.md` | 只读最近 **10 条** | 旧踩坑已内化为共识 |
| `architecture.md` | 完整读取 | 不会频繁更新，大小稳定 |

## 触发关键词

用户说以下任何内容时，立即执行对应操作：
- "加载上下文"、"恢复进度"、"上次做到哪了"、"继续工作" → 规则 1
- "保存上下文"、"保存进度"、"记录一下" → 规则 5
- "检查健康度"、"是不是上下文不够了" → 检查对话轮数和回复质量
- 任何对话的第一句话 → 自动加载相关上下文

## 分支上下文隔离（SVN + Git）

**SVN（分支是物理目录）：** 上下文在 `$(pwd)/.claude/context/`，只在当前目录。

**Git（分支共享工作目录）：** 用 `git rev-parse --abbrev-ref HEAD` 获取当前分支，上下文在 `.claude/context/{分支名}/`。

```
.claude/context/
├── main/                 ← main 分支上下文
├── feature-payment/      ← feature-payment 分支上下文
└── architecture.md       ← 项目架构（所有分支共享）
```

- 切换分支自动切换上下文
- 首次进入新分支自动初始化
- `.claude/context/` 建议 gitignore（本地工作记录，不提交）
- 跨分支任务："给 feature-payment 分支派个排查任务"
