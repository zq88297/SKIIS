---
description: SKIIS 跨项目任务派发 — 发现下游项目问题时自动生成排查任务
globs: **/*
alwaysApply: true
---

# 跨项目/跨模块任务派发

## 触发条件

| 触发方式 | 示例 |
|---------|------|
| 用户主动 | "这个问题可能和 B 项目有关，生成排查任务"、"给 B 派个任务" |
| AI 检测 | 排查中发现根因指向另一个项目时，主动询问 |

## 派发流程（在来源项目 A 中执行）

1. 确认目标项目 B 的路径
2. 自动提取当前上下文：问题背景、A→B 交互方式、关键接口、已知线索
3. 如果 B 的 `docs/ai-context/` 不存在 → 自动初始化
4. 在 B 中生成任务文件 `docs/ai-context/tasks/from-A-{date}-{summary}.md`：

```markdown
# 来自「{项目A}」的排查任务
> 派发时间：{datetime}  |  来源：{项目A路径}

## 问题背景
{在项目A中遇到了什么问题}

## A→B 交互关系
- 调用方式 / 关键接口 / 数据流

## 排查范围
- [ ] {检查项1}
- [ ] {检查项2}

## 已知线索
{日志、返回值、现象}

## 相关文件
{涉及的关键文件路径}
```

5. 在 A 的 `current-task.md` 记录：`📤 已派发到 B`

## 接收流程（在目标项目 B 中）

当打开项目 B 时，优先检查 `docs/ai-context/tasks/` 下的未处理任务并展示。

## A 直接排查 B 的情况

如果不开 B 的会话，直接在 A 中排查 B 的代码，**无论是否有结论都必须回写 B 的文件**：

| 情况 | 操作 |
|------|------|
| 有结论/修了 bug | 更新 B 的 current-task.md + pitfalls.md |
| 确认不是 B 的问题 | 任务移到 tasks/done/，追加排除记录 |
| 没结论但查过了 | 在 task 文件中追加排查记录（已查范围、排除项、剩余疑点） |

这样 B 打开时不会看到空任务重新排查。**避免重复做工。**

## 跨多层嵌套

支持任意深度——从"程序A/子系统/子模块A1"派发到"程序B/子系统/子模块B2"。目标目录不存在则自动初始化。
