---
description: SKIIS 任务调度器 — 分析任务依赖关系，规划并行/串行执行顺序
globs: **/*
alwaysApply: true
---

# 任务调度与并行执行

## 规则 0：生成执行计划后自动分析（最高优先级）

**每次 AI 生成了包含 2 个以上步骤的执行计划，必须自动分析并行化：**

1. 分析每个步骤的依赖（文件冲突、函数冲突、输出依赖）
2. 在计划末尾追加并行分析结果：
```
⚡ 可并行：N 个任务可同时执行
  第 1 组：Task-A, Task-B（独立文件/模块）
  第 2 组：Task-C（依赖 Task-A 完成）
  建议：启动 2 个并行会话，效率提升 2x
```
3. **不需要用户手动触发。**

## 触发

用户说"规划任务"、"分析依赖"、"并行执行"、"哪些任务可以同时做"、"多任务"、"批量"、"一起做"、"一起改"、"分步"时也可以手动触发。

## 依赖判断规则

| 规则 | 结论 |
|------|------|
| 两个任务修改相同的文件 | 🔴 串行 |
| 两个任务修改相同的函数/模块 | 🔴 串行 |
| B 明确需要 A 的输出 | 🔴 串行，A 先 B 后 |
| A 改 `src/ike/`，B 改 `src/log/` | 🟢 可并行 |
| 不同项目 | 🟢 可并行 |
| 任务只读文件 | 🟢 可并行 |

## 执行计划格式

```
📊 任务执行计划
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

第 1 组（并行 - 3 个任务可同时执行）
  🟢 Task-A（独立模块）
  🟢 Task-B（只读文件）
  🟢 跨模块排查（独立项目）

第 2 组（等第 1 组完成后）
  🔴 Task-C（依赖 Task-A 的输出）

📈 效率：原本 6 轮 → 2 轮，提升 3x
```

## 并发限制

- **最多 3 个任务同时执行**
- 完成的会话自动检查队列，领取下一个依赖已满足的任务
- 依赖未满足 → 提示用户等待哪个任务

## 文件写入策略（防冲突）

**每个任务写自己的结果文件，最后一次性合并，零锁竞争：**

```
并行执行中：
  Task-A 完成 → tasks/results/task-A.result.md（独自文件）
  Task-B 完成 → tasks/results/task-B.result.md（独自文件）

下次对话加载时（或用户触发合并时）：
  → 读取所有 .result.md
  → 一次性更新 current-task.md / decisions.md / pitfalls.md
  → 结果文件移到 results/merged/
```

## 任务认领锁

开始任何任务前，检查 `docs/ai-context/tasks/claims/{task-id}.claim`：
- 不存在 → 创建 claim，开始执行
- 存在且 < 24 小时 → 已被认领，跳过
- 存在且 > 24 小时 → 僵尸锁，询问是否接管

## 编译环境自动归档

安装工具链/依赖时，自动记录：
- 成功 → `decisions.md`（工具名、版本、安装命令）
- 失败 → `pitfalls.md`（错误、根因、解决方案）
