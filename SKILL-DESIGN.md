# SKIIS 技能设计文档

> 将此文档交给 CodeX，让它为你生成对应格式的 skill。

---

## 1. 核心目标

让 AI 编程助手拥有**跨会话持久化记忆**，解决两个核心问题：
- 会话长了 AI 会变笨（上下文窗口满了）
- 换新会话后丢失之前的任务进度、技术决策、踩坑记录

---

## 2. 架构概览

```
三层自动化：

第一层：常驻规则（每次对话自动执行）
  - 对话开始时自动加载上次的工作进度
  - 新项目自动初始化上下文系统
  - 对话超过 15 轮自动提醒保存
  - 修复 bug/做决策后自动提醒归档

第二层：用户命令（用户手动精确控制）
  - 保存进度 / 加载上下文 / 结束会话 / 健康检查 / 架构同步

第三层：文件事件触发（轻量自动检查）
  - 检测到新依赖安装 → 提醒同步架构文档
  - 检测到新项目未初始化 → 提醒初始化
```

---

## 3. 上下文文件系统（核心）

所有上下文存储在项目根目录的 `.claude/context/` 下，这是**纯 markdown 文件**，可被任何 AI 工具读取。

| 文件 | 用途 | 更新方式 |
|------|------|---------|
| `current-task.md` | 当前任务进度（含 checkbox） | **覆盖**更新 |
| `decisions.md` | 所有技术决策记录 | **追加**（不覆盖历史） |
| `pitfalls.md` | 踩坑记录和解决方案 | **追加**（不覆盖历史） |
| `architecture.md` | 项目架构概览 | 自动生成 / 覆盖更新 |
| `tasks/` | 跨项目/模块排查任务 | 按需创建 |
| `tasks/claims/` | 并行任务认领锁 | 按需创建 |
| `tasks/results/` | 并行任务结果文件 | 按需创建 |

### current-task.md 的结构

```markdown
> 最后更新: 2026-06-02 14:30

# 当前任务

## 已完成
- [x] 修改了 src/ike/sa.h 的状态枚举
- [x] 编译通过

## 进行中
- [ ] 实现 IKE 协商超时重试逻辑 — 已写完主流程，handle_timeout() 回调未注册

## 待完成
- [ ] 添加单元测试
- [ ] 更新 API 文档

## 🔜 下次继续
### 第一步
打开 src/ike/negotiate.c，在 ike_handle_sa() 中注册 handle_timeout() 回调

### 当前状态
主流程已写完，编译通过

### 关键约束
超时时间必须从配置文件读取，不能硬编码

### 阻塞项
等待运维确认 keepalive 间隔参数

## 关键上下文
- API 端点: /api/v2/ike/negotiate
- 配置文件: /etc/ike/ike.conf
- 依赖库: libopenssl 3.0
```

### decisions.md 的格式

```markdown
# 技术决策记录

## D2026-06-01-1: 选择 libopenssl 而非 libressl
- **背景**: 需要 SSL/TLS 支持
- **选项**: OpenSSL、LibreSSL、mbedTLS
- **选择**: OpenSSL 3.0
- **原因**: 项目已有依赖、社区活跃、文档完善
- **明确拒绝**: LibreSSL（API 不兼容）、mbedTLS（性能不足）
- **取舍**: OpenSSL 体积较大，但可接受

## D2026-06-02-1: 超时重试使用指数退避
...
```

### pitfalls.md 的格式

```markdown
# 踩坑记录

## P2026-06-01-1: IKE SA 状态机死锁
- **现象**: 协商超时后 SA 状态卡在 NEGOTIATING，无法重试
- **根因**: handle_timeout() 未获取互斥锁就直接修改 sa->state
- **解决**: 在 handle_timeout() 开头加 pthread_mutex_lock(&sa_mutex)
- **命令**: git diff commit abc123
- **预防**: 所有修改 sa 状态的地方都检查是否持锁
```

---

## 4. 六条自动规则

### 规则 0：智能加载（最高优先级，每次对话自动触发）

**触发条件**：任何开发相关的问题（"修改"、"排查"、"debug"、"帮我"、"怎么"等）

**执行逻辑**：

```
用户提问
  │
  ├─ 项目简单（无子模块目录）？
  │   ├─ .claude/context/ 存在 → 读取 current-task.md + decisions + pitfalls
  │   │     ├─ 有 "🔜 下次继续" 段落 → 优先展示
  │   │     └─ 无 → 展示任务概览
  │   └─ 不存在 → 自动初始化（扫描项目结构，创建所有模板文件）
  │
  └─ 项目复杂（有多个子模块目录）？
      ├─ 只 ls 一级目录（1秒），不深入读取
      ├─ 根据用户问题判断涉及哪个子模块
      ├─ 该子模块有多个分支？→ 列出分支让用户选
      └─ 加载目标分支的上下文 → 回答用户
```

**关键点**：先理解问题再加载，绝不盲目全量加载。

### 规则 1：新项目自动初始化

`.claude/context/` 不存在时 → 不等用户确认，直接执行：
1. ls 顶层目录，识别项目类型（C/Python/Go/前端）
2. 创建 `.claude/context/` + 模板文件
3. 如果项目有源码，扫描生成 `architecture.md`
4. 安装 hooks（检测文件变化的脚本）

### 规则 2：对话疲劳自动预警

- 对话超过 15 轮 → 提醒保存进度
- AI 发现自身回复矛盾 / 不确定文件路径 / 重复建议 → 警告用户

### 规则 3：工作产出自动归档

| 操作 | 自动提醒 |
|------|---------|
| 修复了一个 bug | "是否记录到 pitfalls.md？" |
| 做了一个技术决策 | "是否追加到 decisions.md？" |
| 完成了一个功能模块 | "是否更新 current-task.md？" |
| 生成了多步骤计划 | 自动转为 current-task.md 的 checkbox 清单 |
| 安装了新工具/依赖 | 自动记录到 decisions.md 或 pitfalls.md |

### 规则 4：保存时必须写继续计划

保存进度时，必须在 `current-task.md` 中写入 `## 🔜 下次继续` 段落：
- **第一步**：打开 X 文件，定位到 Y 函数，做 Z（必须具体到文件和函数）
- **当前状态**：编译/运行状态
- **关键约束**：需要注意的限制
- **阻塞项**：等待确认的事项

### 规则 5：SVN/Git 分支隔离

- **SVN**：上下文在 `$(pwd)/.claude/context/`，只在当前工作目录
- **Git**：上下文在 `.claude/context/{分支名}/`，用 `git rev-parse --abbrev-ref HEAD` 获取分支名
- 切换分支自动切换上下文
- 首次进入新分支自动初始化

---

## 5. 跨项目/跨模块任务派发

**场景**：在项目 A 中排查问题，发现根因可能在项目 B。

**流程**：

```
A 的会话中：
  ├─ 用户说："给 B 派个排查任务"
  ├─ AI 提取当前上下文（问题背景、A→B 交互方式、关键接口、已知线索）
  ├─ B 的 .claude/context/ 不存在 → 自动初始化
  ├─ 在 B 中生成 .claude/context/tasks/from-A-{日期}.md
  └─ 在 A 的 current-task.md 记录：📤 已派发到 B

B 的会话中（下次打开时）：
  └─ 加载时优先展示：📨 来自「A」的排查任务
```

**关键机制**：
无论是否有结论，都必须回写 B 的任务文件：
- 找到 bug → 记录到 B/pitfalls.md
- 做了决策 → 记录到 B/decisions.md
- 确认不是 B 的问题 → 任务移到 done/，追加排除记录
- 没结论但查过了 → 在任务文件追加排查进度

**防止重复做工**：B 打开时看到的不是空白任务，而是"有人查过了"的状态。

---

## 6. 任务并行调度

**场景**：AI 生成了多步骤计划，自动分析哪些可以并行执行。

**依赖判断**：
- 改相同文件/函数 → 🔴 串行
- 有输出依赖 → 🔴 串行
- 不同模块/只读操作 → 🟢 可并行
- 不同项目 → 🟢 可并行

**并发限制**：最多 3 个并行会话

**认领锁**：开始任务前在 `tasks/claims/` 创建认领文件，防止两个会话抢同一任务。

**结果合并**：每个任务写自己的结果文件 `tasks/results/{task-id}.result.md`，下次加载时一次性合并到 `current-task.md`（零锁竞争）。

**自动接续**：一个任务完成后自动检查队列，领取下一个依赖已满足的任务。

---

## 7. 分层上下文（大型多模块项目）

```
大型项目/
├── .claude/context/               # 项目级（模块清单 + 全局概览）
├── 子模块A/
│   └── .claude/context/           # 模块级（A 的职责、接口、任务）
└── 子模块B/
    └── .claude/context/           # 模块级（B 的职责、接口、任务）
```

- 在哪层打开就在哪层加载，互不干扰
- 根目录首次 load 全量扫描（10-30 秒），后续只做 1 秒 diff
- 子模块识别标准：有独立构建配置（Makefile 等）、有 main 入口、目录名暗示独立功能
- 自动排除：src/、tests/、docs/、node_modules/ 等通用目录

---

## 8. 数据结构总结

```
.claude/context/
├── current-task.md          ← 覆盖更新，含 checkbox + 下次继续
├── decisions.md             ← 追加，格式：D{日期}-{序号}: {标题}
├── pitfalls.md              ← 追加，格式：P{日期}-{序号}: {标题}
├── architecture.md          ← 覆盖更新，自动生成
├── tasks/                   ← 跨项目任务
│   ├── from-{来源}-{日期}.md
│   ├── done/
│   ├── claims/              ← 并行任务认领锁
│   │   └── {task-id}.claim
│   ├── results/             ← 并行任务结果
│   │   └── {task-id}.result.md
│   └── queue/               ← 排队等待的任务
└── .locks/                  ← 文件写入锁（已废弃，用结果文件替代）
```

---

## 9. 关键设计决策

| 决策 | 原因 |
|------|------|
| `current-task.md` 覆盖而非追加 | 保持文件简洁，不需要翻历史 |
| `decisions.md` 和 `pitfalls.md` 追加 | 保留完整历史，方便回溯 |
| 用纯 markdown 文件不依赖数据库 | 跨 IDE 兼容，git 可追踪，AI 直接读取 |
| 结果文件 + 合并而非文件锁 | 避免并发写入竞争，零锁等待 |
| 先理解问题再加载上下文 | 大型项目不盲目全量扫描 |
| 分支隔离（Git 用子目录，SVN 用物理目录） | 上下文跟分支走，互不干扰 |
| 认领锁而非全局调度器 | 简单可靠，文件系统天然适合 |

